node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage

# Environment / secrets (keep .env.example tracked)
.env
.env.*
!.env.example

# Local reference-extraction working files (PDFs are NOT committed)
/data-extraction/_raw/
*.pdf

# Machine-specific Claude Code permission state (references local paths)
.claude/settings.local.json
